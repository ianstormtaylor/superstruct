import { struct } from '../../..'

export const Struct = struct.instance(Array)

export const data = false

export const error = {
  path: [],
  value: false,
  type: 'instance<Array>',
  reason: null,
}
