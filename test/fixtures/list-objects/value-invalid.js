
import { struct } from '../../..'

export const Struct = struct([{ id: 'string' }])

export const data = 'invalid'

export const error = {
  code: 'value_invalid',
  type: 'array',
  path: [],
  value: 'invalid',
}
