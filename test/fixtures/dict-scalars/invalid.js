
import { struct } from '../../..'

export const Struct = struct.dict(['string', 'number'])

export const data = 'invalid'

export const error = {
  path: [],
  value: 'invalid',
  type: 'dict<string,number>',
}
