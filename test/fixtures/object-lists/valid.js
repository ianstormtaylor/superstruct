
import { struct as s } from '../../..'

export const struct = s({
  title: 'string',
  tags: ['string'],
})

export const value = {
  title: 'hello world',
  tags: ['news'],
}

export const output = {
  title: 'hello world',
  tags: ['news'],
}
