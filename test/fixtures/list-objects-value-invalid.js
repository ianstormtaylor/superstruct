
import s from '../..'

export const struct = s([{ id: 'string' }])

export const value = 'invalid'

export const error = {
  code: 'value_invalid',
  type: 'array',
  path: [],
  value: 'invalid',
}
