import { struct } from '../../..'

export const Struct = struct.notEmptyList([{ id: 'string' }])

export const data = []

export const error = {
  path: [],
  value: [],
  type: '[{id}]',
  reason: null,
}
