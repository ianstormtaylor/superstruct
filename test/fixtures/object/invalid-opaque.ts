import { object } from '../../..'

export const Struct = object()

export const data = 'invalid'

export const failures = [
  {
    value: 'invalid',
    type: 'Object',
    path: [],
    branch: [data],
  },
]
