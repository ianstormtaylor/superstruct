
import { struct as s } from '../../..'

export const struct = s('number')

export const value = 'invalid'

export const error = {
  code: 'value_invalid',
  type: 'number',
  path: [],
  value: 'invalid',
}
