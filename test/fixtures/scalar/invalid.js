
import { struct } from '../../..'

export const Struct = struct('number')

export const data = 'invalid'

export const error = {
  code: 'value_invalid',
  type: 'number',
  path: [],
  value: 'invalid',
}
