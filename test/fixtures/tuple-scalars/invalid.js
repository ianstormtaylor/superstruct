import { struct } from '../../..'

export const Struct = struct(['string', 'number'])

export const data = 'invalid'

export const error = {
  path: [],
  value: 'invalid',
  type: '[string,number]',
}
