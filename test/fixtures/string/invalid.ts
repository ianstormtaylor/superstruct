import { string } from '../../..'

export const Struct = string()

export const data = false

export const failures = [
  {
    value: false,
    type: 'string',
    path: [],
    branch: [data],
  },
]
