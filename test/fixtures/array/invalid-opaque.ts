import { array } from '../../..'

export const Struct = array()

export const data = 'invalid'

export const failures = [
  {
    value: 'invalid',
    type: 'Array<unknown>',
    path: [],
    branch: [data],
  },
]
