import { date } from '../../..'

export const Struct = date()

export const data = 'invalid'

export const failures = [
  {
    value: 'invalid',
    type: 'Date',
    path: [],
    branch: [data],
  },
]
