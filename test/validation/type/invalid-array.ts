import { type } from '../../../src'

export const Struct = type({})

export const data = []

export const failures = [
  {
    value: [],
    type: 'type',
    refinement: undefined,
    path: [],
    branch: [data],
  },
]
