import { date } from '../../..'

export const Struct = date()

export const data = 'invalid'

export const error = {
  value: 'invalid',
  type: 'date',
  refinement: undefined,
  path: [],
  branch: [data],
}
