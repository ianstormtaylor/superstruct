import { date } from '../../..'

export const Struct = date()

export const data = 'invalid'

export const failures = [
  {
    value: 'invalid',
    type: 'date',
    refinement: undefined,
    path: [],
    branch: [data],
  },
]
