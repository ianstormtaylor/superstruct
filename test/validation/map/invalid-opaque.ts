import { map } from '../../..'

export const Struct = map()

export const data = 'invalid'

export const failures = [
  {
    value: 'invalid',
    type: 'map',
    refinement: undefined,
    path: [],
    branch: [data],
  },
]
