import { never } from '../../..'

export const Struct = never()

export const data = true

export const failures = [
  {
    value: true,
    type: 'never',
    refinement: undefined,
    path: [],
    branch: [data],
  },
]
