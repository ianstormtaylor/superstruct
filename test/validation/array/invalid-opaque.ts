import { array } from '../../..'

export const Struct = array()

export const data = 'invalid'

export const error = {
  value: 'invalid',
  type: 'array',
  refinement: undefined,
  path: [],
  branch: [data],
}
