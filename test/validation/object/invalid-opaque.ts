import { object } from '../../..'

export const Struct = object()

export const data = 'invalid'

export const error = {
  value: 'invalid',
  type: 'object',
  refinement: undefined,
  path: [],
  branch: [data],
}
