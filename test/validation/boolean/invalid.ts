import { boolean } from '../../..'

export const Struct = boolean()

export const data = 'invalid'

export const error = {
  value: 'invalid',
  type: 'boolean',
  refinement: undefined,
  path: [],
  branch: [data],
}
