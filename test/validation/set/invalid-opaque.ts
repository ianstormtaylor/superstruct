import { set } from '../../../src';

export const Struct = set();

export const data = 'invalid';

export const failures = [
  {
    value: 'invalid',
    type: 'set',
    refinement: undefined,
    path: [],
    branch: [data],
  },
];
