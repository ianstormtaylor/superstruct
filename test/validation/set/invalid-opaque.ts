import { set } from '../../..'

export const Struct = set()

export const data = 'invalid'

export const error = {
  value: 'invalid',
  type: 'set',
  refinement: undefined,
  path: [],
  branch: [data],
}
